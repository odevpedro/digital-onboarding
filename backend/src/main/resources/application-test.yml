spring:
  datasource:
    url: jdbc:tc:postgresql:15-alpine:///onboarding
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
